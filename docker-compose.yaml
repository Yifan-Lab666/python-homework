version: '3.8'

services:
  hero-route-app:
    build: .
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/__pycache__
    command: python hero_route_diff_rebuild.py
    restart: unless-stopped